Ext.data.Types.Uri = {
    type: 'uri',
    convert: function(v, data) {
        // temporary function until a use case is defined around
        // how the uri should be converted.
        return v;
    }
};